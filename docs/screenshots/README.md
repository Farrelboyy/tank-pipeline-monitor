Place screenshot files here to display them in the README.

Recommended filenames:
- dashboard-overview.png
- tank-detail.png
- alerts-page.png

These files are intentionally excluded from .gitignore so screenshots can be committed.
To capture them, run both servers, open the dashboard in your browser, and take
screenshots of each page. Then move the image files into this folder.
